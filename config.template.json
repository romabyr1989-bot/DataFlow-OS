{
  "data_dir":       "${DATA_DIR}",
  "plugins_dir":    "${PLUGINS_DIR}",
  "port":           ${PORT},
  "auth_enabled":   1,
  "jwt_secret":     "${JWT_SECRET}",
  "admin_password": "${ADMIN_PASSWORD}",
  "tls_cert":       "${TLS_CERT}",
  "tls_key":        "${TLS_KEY}"
}
